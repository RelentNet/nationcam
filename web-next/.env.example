# Absolute base URL the SSR server uses to reach the Go API. The browser always
# talks to the same-origin `/api` proxy, so this is server-side only.
# In production point it at the internal address nginx proxies to.
# Unset in dev, where it falls back to production's public read-only API.
API_URL=http://api:8080
